prometheus:
  prometheusSpec:
    retention: 6h
    retentionSize: "1GB"
    storageSpec: {}
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"

alertmanager:
  enabled: false
